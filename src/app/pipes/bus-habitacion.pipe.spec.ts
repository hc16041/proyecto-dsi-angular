import { BusHabitacionPipe } from './bus-habitacion.pipe';

describe('BusHabitacionPipe', () => {
  it('create an instance', () => {
    const pipe = new BusHabitacionPipe();
    expect(pipe).toBeTruthy();
  });
});
